# Contract Cockpit v7.6 — runtime PWA

This is the runtime-complete static PWA. Serve the directory over HTTP/HTTPS and open `cockpit/index.html`. It intentionally contains no test toolchain. Use the separate Source-Audit package to reproduce test and checksum claims.

Contract analysis and matter state remain local to the browser profile; the application contains no cloud contract-processing or telemetry path.
